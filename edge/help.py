#!/usr/bin/env python3
import sys

def print_help():
    guide = """
======================================================================
                  AMEVA Edge Agent - 종합 매뉴얼 (A-Z Guide)
======================================================================

본 프로그램은 모바일 기기(Termux) 환경에서 동작하는 초경량, 고보안 
오디오 처리 및 온디바이스 AI 기반 STT/LLM 요약 파이프라인입니다.

----------------------------------------------------------------------
[1] 핵심 시스템 아키텍처 (Architecture)
----------------------------------------------------------------------
1. 오디오 탐지 (scan): audio/ 폴더에 새 녹음 파일 인입 시 DB 등록
2. 오디오 전처리 (stt): ffmpeg 필터 체인으로 노이즈 감쇄 및 무음 영역 제거
3. STT 변환 (stt): whisper.cpp + ggml 모델(base/small) 활용 한국어 인식
4. LLM 요약 (llm): llama.cpp (qwen 0.5b 또는 Llama 3B) 기반 결과 요약
5. 21시 배치 (sync-files): 텍스트/오디오 파일 원격 전송 후 로컬 디스크 파쇄(shred)
6. 23시 배치 (sync-db): SQLite DB 원격 병합 전송 후 로컬 DB 완전 파쇄(shred)
7. 데몬 감시 (daemon): 백그라운드 스케줄러 & crash-recovery 감시 루프

----------------------------------------------------------------------
[2] 환경 설정 파일 (.env) 주요 옵션 가이드
----------------------------------------------------------------------
설정 파일은 ~/dev/ameva-agent/.env 에 위치합니다.

* LLM_ENGINE: "llama" (Llama.cpp 가속 추천) 또는 "bitnet" / "ollama"
* WHISPER_BIN: 빌드된 whisper-cli 바이너리 절대 경로
* WHISPER_MODEL: 사용할 .bin 모델 경로 (base 또는 small 권장)
* LLAMA_BIN: 빌드된 llama-cli 바이너리 절대 경로
* LLAMA_MODEL: 사용할 .gguf 모델 경로 (Qwen 2.5 0.5B 권장 - 튕김 방지)
* SSH_HOST / SSH_USER / SSH_PORT: 수신 대상 백업 윈도우/리눅스 PC의 정보
* SSH_KEY: 원격 전송 시 사용할 폰의 SSH Private Key (~/.ssh/id_rsa)

----------------------------------------------------------------------
[3] 단계별 테스트 및 구동 방법 (Testing Methods)
----------------------------------------------------------------------

1단계: 테스트 파일 준비
  - 폰에서 녹음된 오디오(.m4a, .mp3, .wav 등)를 ~/dev/ameva-agent/audio/ 폴더로 복사합니다.

2단계: CLI 대화형 실행
  $ ./run.sh
  
  - 1번(scan) 선택: 신규 오디오를 스캔하고 전처리 WAV 파일을 생성합니다.
  - 2번(stt)  선택: 오디오 음성인식을 수행하여 정밀 타임스탬프 [.txt] 파일을 만듭니다.
  - 3번(llm)  선택: 로컬 LLM을 호출하여 요약 문서를 만듭니다.
  - 4번(sync-files) 선택: 생성된 텍스트/음성 파일을 윈도우 PC로 안전 전송 후 파쇄 소거합니다.
  - 5번(sync-db)    선택: SQLite DB를 윈도우 PC로 안전 전송 후 로컬에서 완전 영구 파쇄합니다.

3단계: 백그라운드 감시 데몬 실행 (Daemon Loop)
  $ ./run_daemon.sh
  - 백그라운드에서 상시 대기하며, 오류로 중단되어도 5초 내에 자동 재가동됩니다.

----------------------------------------------------------------------
[4] 문제 해결 및 에러 트러블슈팅 (Troubleshooting)
----------------------------------------------------------------------

Q. STT 수행 중 "[MOCK] whisper.cpp 모의 처리"라는 문구가 뜹니다.
A. .env 설정 내 WHISPER_BIN 경로가 올바르지 않은 경우 안전을 위해 모의 처리로 우회됩니다.
   setup.sh를 재실행하거나 .env의 WHISPER_BIN 경로를 다시 확인해 주세요.

Q. LLM 실행 중 "llama-cli 에러 코드 -9" 또는 자꾸 튕깁니다.
A. 메모리 부족(OOM)으로 인한 안드로이드 OS의 강제 프로세스 킬 현상입니다.
   Llama 3.2 3B 모델 대신 경량 모델인 Qwen 2.5 0.5B 모델을 설정해 주시면 즉시 해결됩니다.

Q. 21시/23시 동기화(sync) 실행 시 SSH 연결 오류가 발생합니다.
A. 폰의 공개키(~/.ssh/id_rsa.pub)가 윈도우 PC의 C:\\Users\\<계정>\\.ssh\\authorized_keys에
   올바르게 등록되었는지 확인해 주십시오.

======================================================================
"""
    print(guide)

if __name__ == "__main__":
    print_help()
